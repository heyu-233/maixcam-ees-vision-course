# 05 角点排序与真实四边形

本节目标：

- 从上一节保留的 `approx` 中取出真实四边形角点。
- 将四个角点排序为 `TL, TR, BR, BL`。
- 使用排序后的角点绘制真实四边形。
- 标出四个角点标签。
- 计算真实四边形中心点。

运行方法：

1. 用 MaixVision 打开本目录。
2. 运行 `main.py`。
3. 对准矩形目标，屏幕会显示真实四边形边框、角点标签和中心点。

这一节用于承接矩形识别，并为后续透视变换做准备。

