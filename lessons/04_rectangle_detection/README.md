# 04 矩形筛选与中心点

本节目标：

- 在轮廓中筛掉面积太小的干扰。
- 使用 `cv2.approxPolyDP()` 将轮廓近似成多边形。
- 只保留四边形候选。
- 使用长宽比继续过滤。
- 选择面积最大的矩形作为目标。
- 在屏幕上画出外接矩形和中心点。

运行方法：

1. 用 MaixVision 打开本目录。
2. 运行 `main.py`。
3. 对准矩形目标，屏幕会显示绿色外接框、红色中心点和坐标信息。

这一节对应“矩形识别逐步开发讲解”的第 7 步到第 12 步。

